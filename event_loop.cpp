/*
 * @Author: zhuxiaolong 
 * @Date: 2019-12-04 09:50:16 
 * @Last Modified by: zhuxiaolong
 * @Last Modified time: 2019-12-04 10:09:11
 */

#include "event_loop.h"

#include <sys/types.h>

#include <vector>

#include "base/basic_types.h"
#include "FwCommon.h/public/bar.h"

using std::string

namespace my_name_space {
  EventLoop::EventLoop()
      : num_entries_(10),
        num_complated_connections_(false) {
          //
        }


ReturnType ClassName::ReallyLongFunctionName(const Type& par_name1,
                                             Type* par_name2) {
  bool retval = DoSomething(averyveryveryaveryveryveryaveryveryveryargument1,
                            argument2, argument3);
  if (condition) {
    for (int i = 0; i < kSomeNumber; ++i) {
      if (this_one_thing > this_ohter_thing &&
          a_third_thing == a_fourth_thing) {
            //TODO:(@zhuxiaolong): add the parking thing 
      }
    } else {
      nt j = g();
    }
    switch (var) {
      case 0: {
        //TODO:(@chen) : add apa process
        break;
      }
      default : {
        assert(false);
      }
    }
  }
  return x;
}

} // namespace my_name_space